{% import '@server_theme/templates/server-theme-bg-color-base.html.twig' as bgColorBase %}

<div class="border-t flex flex-row justify-around gap-1 bg-gray-100">
  {% for link in links %}
    <div class="{{ bgColorBase.getBgColor(bg_color) }} w-full">{{ link }}</div>
  {% endfor %}
</div>
